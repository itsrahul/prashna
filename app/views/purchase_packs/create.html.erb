<h2>Thank you for payment  </h2>

<%# @packs.each do |@pack| %>

<div class="card" style="max-width: 150rem;">
  <div class="card-body">
    <h4 class="card-title"><%= @pack.name %></h4>
    <h6 class="card-subtitle mb-2 text-muted"><%= number_to_currency @pack.price, unit: 'Rs.' %></h6>
    <p class="card-text"><%= @pack.value %></p>
    <%#= button_to "Buy", checkout_create_path(@pack.id), class: "card-link" %>
  </div>
</div>
<%# end %>
<br>
